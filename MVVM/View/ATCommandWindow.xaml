<Window x:Class="ELM327_GUI.MVVM.View.ATCommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ELM327_GUI.MVVM.View"
        mc:Ignorable="d"
        Title="ATCommandWindow" Height="150" Width="400">
    <Grid>
		<StackPanel Margin="10">
			<TextBlock Text="Írd be az AT parancsot:" FontSize="14" Margin="0,0,0,5"/>
			<TextBox x:Name="ATCommandTextBox" Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Margin="0,0,0,10" />
			<Button Content="Küldés" Height="25" Width="80" HorizontalAlignment="Right" Command="{Binding SendCommand}"
					CommandParameter="{Binding CommandText}"/>
		</StackPanel>

	</Grid>
</Window>
