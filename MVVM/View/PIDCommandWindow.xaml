<Window x:Class="ELM327_GUI.MVVM.View.PIDCommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ELM327_GUI.MVVM.View"
        mc:Ignorable="d"
        Title="PIDCommandWindow" Height="160" Width="400">
    <Grid>
		<StackPanel Margin="10">
			<TextBlock Text="Írd be a PID parancsot vagy válassza a listából!:" FontSize="14" Margin="0,0,0,5"/>
			<TextBox x:Name="PIDCommandTextBox" Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Margin="0,0,0,10" />
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
			<Button Content="Küldés" Width="75" HorizontalAlignment="Right" Command="{Binding SendCommand}"
					CommandParameter="{Binding CommandText}"/>
			<Button Content="Lista" Width="75" HorizontalAlignment="Left" Command="{Binding ShowListCommand}"/>
			</StackPanel>

		</StackPanel>
        
    </Grid>
</Window>
